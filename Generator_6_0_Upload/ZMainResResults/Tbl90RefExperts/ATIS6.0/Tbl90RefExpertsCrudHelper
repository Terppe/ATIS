    
        public ObservableCollection<NULL> SearchForConnectedDatasetsWithRefExpertIdInTableFiSpecies(Tbl90RefExpert selected)
        {
            var collection = new ObservableCollection<NULL>(_uow.Tbl69FiSpeciesses.Find(x => x.RefExpertId == selected.RefExpertId));
            return collection;
        }


        public ObservableCollection<Tbl90Reference> DeleteDatasetsWithRefExpertIdInTableReference(Tbl90RefExpert selected)
        {
            var collection = new ObservableCollection<Tbl90Reference>(_uow.Tbl90References.Find(x => x.RefExpertId == selected.RefExpertId));
            return collection;
        }

        public ObservableCollection<Tbl93Comment> DeleteDatasetsWithRefExpertIdInTableComment(Tbl90RefExpert selected)
        {
            var collection = new ObservableCollection<Tbl93Comment>(_uow.Tbl93Comments.Find(x => x.RefExpertId == selected.RefExpertId));
            return collection;
        }

        public void DeleteRefExpert(Tbl90RefExpert selected)
        {
            _uow.Tbl90RefExperts.Remove(selected);
            _uow.Complete();
        }

        public ObservableCollection<T> GetCollectionOrderByFromNULLId<T>(int id)
        {
            ObservableCollection<T> collection;
            collection = new ObservableCollection<T>((IEnumerable<T>)_context.NULL
                .Where(e => e.NULLId == id)
                .OrderBy(k => k.NULLName));

            return collection;
        }

        public ObservableCollection<T> GetRefExpertsCollectionOrderByFromRefExpertId<T>(int id)
        {
            ObservableCollection<T> collection;
            collection = new ObservableCollection<T>((IEnumerable<T>)_context.Tbl90RefExperts
                .Where(e => e.RefExpertId == id)
                .OrderBy(k => k.RefExpertName));
            return collection;
        }

        public ObservableCollection<T> GetCollectionOrderByFromSubphylumId<T>(int id)
        {
            ObservableCollection<T> collection;
            collection = new ObservableCollection<T>((IEnumerable<T>)_context.Tbl69FiSpeciesses
                .Where(e => e.RefExpertId == id)
                .OrderBy(k => k.FiSpeciesName));
            return collection;
        }

        public ObservableCollection<T> GetReferenceExpertsCollectionOrderByFromRefExpertIdAndRefAuthorIdIsNullAndRefSourceIdIsNull<T>(int id)
        {
            ObservableCollection<T> collection;
            collection = new ObservableCollection<T>((IEnumerable<T>)_context.Tbl90References
                .Include(a => a.Tbl90RefAuthors)
                .Where(e => e.RefExpertId == id && e.RefAuthorId == null && e.RefSourceId == null)
                .OrderBy(k => k.Info));
            return collection;
        }

        public ObservableCollection<T> GetReferenceSourcesCollectionOrderByFromRefExpertIdAndRefAuthorIdIsNullAndRefExpertIdIsNull<T>(int id)
        {
            ObservableCollection<T> collection;
            collection = new ObservableCollection<T>((IEnumerable<T>)_context.Tbl90References
                .Include(a => a.Tbl90RefSources)
                .Where(e => e.RefExpertId == id && e.RefAuthorId == null && e.RefExpertId == null)
                .OrderBy(k => k.Info));
            return collection;
        }

        public ObservableCollection<T> GetReferenceAuthorsCollectionOrderByFromRefExpertIdAndRefSourceIdIsNullAndRefExpertIdIsNull<T>(int id)   
        {
            ObservableCollection<T> collection;
            collection = new ObservableCollection<T>((IEnumerable<T>)_context.Tbl90References
                .Include(a => a.Tbl90RefAuthors)
                .Where(e => e.RefExpertId == id && e.RefSourceId == null && e.RefExpertId == null)
                .OrderBy(k => k.Info));
            return collection;
        }

        public ObservableCollection<T> GetCommentsCollectionOrderByFromSubphylumId<T>(int id)
        {
            ObservableCollection<T> collection;
            collection = new ObservableCollection<T>((IEnumerable<T>)_context.Tbl93Comments
                .Where(e => e.RefExpertId == id)
                .OrderBy(k => k.Info));
            return collection;
        }

   
