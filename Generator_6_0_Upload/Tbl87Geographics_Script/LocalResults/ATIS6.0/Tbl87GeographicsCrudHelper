    
        public ObservableCollection<Tbl68Speciesgroup> SearchForConnectedDatasetsWithGeographicIdInTableName(Tbl87Geographic selected)
        {
            var collection = new ObservableCollection<Tbl68Speciesgroup>(_uow.NULL.Find(x => x.GeographicId == selected.GeographicId));
            return collection;
        }


        public ObservableCollection<Tbl90Reference> DeleteDatasetsWithGeographicIdInTableReference(Tbl87Geographic selected)
        {
            var collection = new ObservableCollection<Tbl90Reference>(_uow.Tbl90References.Find(x => x.GeographicId == selected.GeographicId));
            return collection;
        }

        public ObservableCollection<Tbl93Comment> DeleteDatasetsWithGeographicIdInTableComment(Tbl87Geographic selected)
        {
            var collection = new ObservableCollection<Tbl93Comment>(_uow.Tbl93Comments.Find(x => x.GeographicId == selected.GeographicId));
            return collection;
        }

        public void DeleteGeographic(Tbl87Geographic selected)
        {
            _uow.Tbl87Geographics.Remove(selected);
            _uow.Complete();
        }

        public ObservableCollection<T> GetFiSpeciessesCollectionOrderByFromFiSpeciesId<T>(int id)
        {
            ObservableCollection<T> collection;
            collection = new ObservableCollection<T>((IEnumerable<T>)_context.Tbl69FiSpeciesses
                .Where(e => e.FiSpeciesId == id)
                .OrderBy(k => k.FiSpeciesName));

            return collection;
        }

        public ObservableCollection<T> GetGeographicsCollectionOrderByFromGeographicId<T>(int id)
        {
            ObservableCollection<T> collection;
            collection = new ObservableCollection<T>((IEnumerable<T>)_context.Tbl87Geographics
                .Where(e => e.GeographicId == id)
                .OrderBy(k => k.GeographicName));
            return collection;
        }

        public ObservableCollection<T> GetNULLCollectionOrderByFromSubphylumId<T>(int id)
        {
            ObservableCollection<T> collection;
            collection = new ObservableCollection<T>((IEnumerable<T>)_context.NULL
                .Where(e => e.GeographicId == id)
                .OrderBy(k => k.NameName));
            return collection;
        }

        public ObservableCollection<T> GetReferenceExpertsCollectionOrderByFromGeographicIdAndRefAuthorIdIsNullAndRefSourceIdIsNull<T>(int id)
        {
            ObservableCollection<T> collection;
            collection = new ObservableCollection<T>((IEnumerable<T>)_context.Tbl90References
                .Include(a => a.Tbl90RefAuthors)
                .Where(e => e.GeographicId == id && e.RefAuthorId == null && e.RefSourceId == null)
                .OrderBy(k => k.Info));
            return collection;
        }

        public ObservableCollection<T> GetReferenceSourcesCollectionOrderByFromGeographicIdAndRefAuthorIdIsNullAndRefExpertIdIsNull<T>(int id)
        {
            ObservableCollection<T> collection;
            collection = new ObservableCollection<T>((IEnumerable<T>)_context.Tbl90References
                .Include(a => a.Tbl90RefSources)
                .Where(e => e.GeographicId == id && e.RefAuthorId == null && e.RefExpertId == null)
                .OrderBy(k => k.Info));
            return collection;
        }

        public ObservableCollection<T> GetReferenceAuthorsCollectionOrderByFromGeographicIdAndRefSourceIdIsNullAndRefExpertIdIsNull<T>(int id)   
        {
            ObservableCollection<T> collection;
            collection = new ObservableCollection<T>((IEnumerable<T>)_context.Tbl90References
                .Include(a => a.Tbl90RefAuthors)
                .Where(e => e.GeographicId == id && e.RefSourceId == null && e.RefExpertId == null)
                .OrderBy(k => k.Info));
            return collection;
        }

        public ObservableCollection<T> GetCommentsCollectionOrderByFromSubphylumId<T>(int id)
        {
            ObservableCollection<T> collection;
            collection = new ObservableCollection<T>((IEnumerable<T>)_context.Tbl93Comments
                .Where(e => e.GeographicId == id)
                .OrderBy(k => k.Info));
            return collection;
        }

   
