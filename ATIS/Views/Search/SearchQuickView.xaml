<UserControl
    x:Class="ATIS.Ui.Views.Search.SearchQuickView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:ATIS.Ui.Models"
    xmlns:search="clr-namespace:ATIS.Ui.Views.Search"
    Width="Auto"
    Height="Auto"
    MaxHeight="805"
    d:DesignHeight="550"
    d:DesignWidth="1500"
    mc:Ignorable="d">


    <Grid x:Name="LayoutRoot">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
                <search:SearchBase MinHeight="500" />
            </StackPanel>

            <!--<Grid x:Name="LayoutRoot">
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <StackPanel
                Width="Auto"
                MaxHeight="750"
                HorizontalAlignment="Left">
                <StackPanel
                    Width="Auto"
                    Margin="5,0,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    FocusManager.FocusedElement="{Binding ElementName=TbSearch}"
                    Orientation="Horizontal">
                    <TextBlock
                        Margin="5,0,0,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Text="{DynamicResource Shared-Search}" />
                    <TextBox
                        x:Name="TbSearch"
                        Width="170"
                        Height="32"
                        Margin="10,2,5,0"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        mah:TextBoxHelper.ButtonCommand="{Binding TextBoxButtonCmd, Mode=OneWay}"
                        ContextMenu="{x:Null}"
                        FocusManager.FocusedElement="{Binding RelativeSource={RelativeSource Self}}"
                        Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                        ToolTip="Input Name">
                        <TextBox.Style>
                            <Style BasedOn="{StaticResource MahApps.Styles.TextBox}" TargetType="{x:Type TextBox}">
                                <Setter Property="mah:TextBoxHelper.ClearTextButton" Value="True" />
                                <Style.Triggers>
                                    <Trigger Property="mah:TextBoxHelper.HasText" Value="False">
                                        <Setter Property="mah:TextBoxHelper.ClearTextButton" Value="False" />
                                        <Setter Property="mah:TextBoxHelper.Watermark" Value="{DynamicResource Shared-SearchName}" />
                                    </Trigger>
                                    <Trigger Property="mah:TextBoxHelper.HasText" Value="True">
                                        <Setter Property="mah:TextBoxHelper.ClearTextButton" Value="True" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                    <Button
                        Width="30"
                        Height="30"
                        Margin="10,2,20,2"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Command="{Binding RunQuickSearchCommand, Mode=OneWay}"
                        ToolTip="Search">
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <iconPacks:PackIconBoxIcons
                                    Width="18"
                                    Height="18"
                                    Kind="RegularSearch" />
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>

            -->
            <!--<Button
                        Width="120"
                        Margin="10"
                        VerticalAlignment="Center"
                        materialDesign:ShadowAssist.ShadowDepth="Depth1"
                        Command="{Binding RunQuickSearchCommand}"
                        Content="{DynamicResource Shared-Search}"
                        DockPanel.Dock="Right"
                        IsDefault="True"
                        Style="{StaticResource ButtonDefault}" />-->
            <!--
                    <Button
                        Width="120"
                        Margin="250,0,0,0"
                        VerticalAlignment="Center"
                        materialDesign:ShadowAssist.ShadowDepth="Depth1"
                        Click="Print_Click"
                        Content="{DynamicResource Shared-ButtonPrint}"
                        Style="{StaticResource MahApps.Styles.Button.Flat}" />
                    <Button
                        Width="130"
                        Margin="50,0,0,0"
                        VerticalAlignment="Center"
                        materialDesign:ShadowAssist.ShadowDepth="Depth1"
                        Command="{Binding PdfListAllCommand}"
                        Content="{DynamicResource Report-PrintPdf}"
                        Style="{StaticResource MahApps.Styles.Button.Flat}" />
                </StackPanel>

                <TabControl Grid.Row="0"
                        Width="Auto"
                        Height="600"
                        Margin="0,0,0,20">
                    <TabItem Header="{DynamicResource Database-MenuGenus}">
                        <GroupBox>
                            <ListView
                            x:Name="Tbl66GenussesList"
                            IsSynchronizedWithCurrentItem="True"
                            ItemsSource="{Binding GenussesCollection}"
                            ScrollViewer.HorizontalScrollBarVisibility="Auto"
                            ScrollViewer.IsDeferredScrollingEnabled="True"
                            ScrollViewer.VerticalScrollBarVisibility="Auto"
                            VirtualizingStackPanel.IsVirtualizing="True"
                            VirtualizingStackPanel.VirtualizationMode="Recycling">

                                <ListView.ItemContainerStyle>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="Tag" Value="{Binding (models:Tbl66Genus.GenusId)}" />
                                        <EventSetter Event="MouseDoubleClick" Handler="Tbl66GenussesList_DoubleClick" />
                                    </Style>
                                </ListView.ItemContainerStyle>

                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Width="Auto" Header="{DynamicResource Shared-CountId}">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock
                                                    Width="100"
                                                    Text="{Binding (models:Tbl66Genus.CountId)}"
                                                    TextAlignment="Left" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Width="Auto" Header="{DynamicResource Tbl66Genus-GenusId}">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock
                                                    Width="100"
                                                    Text="{Binding (models:Tbl66Genus.GenusId)}"
                                                    TextAlignment="Left" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Width="60" Header="{DynamicResource Shared-Valid}">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal">
                                                        <CheckBox IsChecked="{Binding (models:Tbl66Genus.Valid)}" />
                                                    </StackPanel>
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn
                                        Width="200"
                                        DisplayMemberBinding="{Binding (models:Tbl66Genus.GenusName)}"
                                        Header="{DynamicResource Tbl66Genus-GenusName}" />
                                        <GridViewColumn
                                        Width="200"
                                        DisplayMemberBinding="{Binding (models:Tbl66Genus.Author)}"
                                        Header="{DynamicResource Shared-Author}" />
                                        <GridViewColumn
                                        Width="80"
                                        DisplayMemberBinding="{Binding (models:Tbl66Genus.AuthorYear)}"
                                        Header="{DynamicResource Shared-AuthorYear}" />
                                        <GridViewColumn
                                        Width="200"
                                        DisplayMemberBinding="{Binding (models:Tbl66Genus.EngName)}"
                                        Header="{DynamicResource Shared-EngName}" />
                                        <GridViewColumn
                                        Width="200"
                                        DisplayMemberBinding="{Binding (models:Tbl66Genus.FraName)}"
                                        Header="{DynamicResource Shared-FraName}" />
                                        <GridViewColumn
                                        Width="200"
                                        DisplayMemberBinding="{Binding (models:Tbl66Genus.PorName)}"
                                        Header="{DynamicResource Shared-SpaName}" />
                                        <GridViewColumn
                                        Width="200"
                                        DisplayMemberBinding="{Binding (models:Tbl66Genus.GerName)}"
                                        Header="{DynamicResource Shared-GerName}" />
                                    </GridView>
                                </ListView.View>
                            </ListView>
                        </GroupBox>
                    </TabItem>
                    <TabItem Header="{DynamicResource Database-MenuFiSpecies}">
                        <TabControl Margin="1">
            -->
            <!--  Bind the SelectionChanged event of the tab  -->
            <!--
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="SelectionChanged">
                                    <i:InvokeCommandAction Command="{Binding TabSelectionChangedFiSpeciesCommand}" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                            <GroupBox>
                                <ListView
                                x:Name="Tbl69FiSpeciessesList"
                                IsSynchronizedWithCurrentItem="True"
                                ItemsSource="{Binding FiSpeciessesCollection}"
                                ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                ScrollViewer.IsDeferredScrollingEnabled="True"
                                ScrollViewer.VerticalScrollBarVisibility="Auto"
                                VirtualizingStackPanel.IsVirtualizing="True"
                                VirtualizingStackPanel.VirtualizationMode="Recycling">

                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem">
                                            <Setter Property="Tag" Value="{Binding (models:Tbl69FiSpecies.FiSpeciesId)}" />
                                            <EventSetter Event="MouseDoubleClick" Handler="Tbl69FiSpeciessesList_DoubleClick" />
                                        </Style>
                                    </ListView.ItemContainerStyle>

                                    <ListView.View>
                                        <GridView>
                                            <GridViewColumn Width="Auto" Header="{DynamicResource Shared-CountId}">
                                                <GridViewColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <TextBlock
                                                        Width="100"
                                                        Text="{Binding (models:Tbl69FiSpecies.CountId)}"
                                                        TextAlignment="Left" />
                                                    </DataTemplate>
                                                </GridViewColumn.CellTemplate>
                                            </GridViewColumn>
                                            <GridViewColumn Width="Auto" Header="{DynamicResource Tbl69FiSpecies-FiSpeciesId}">
                                                <GridViewColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <TextBlock
                                                        Width="100"
                                                        Text="{Binding (models:Tbl69FiSpecies.FiSpeciesId)}"
                                                        TextAlignment="Left" />
                                                    </DataTemplate>
                                                </GridViewColumn.CellTemplate>
                                            </GridViewColumn>
                                            <GridViewColumn Width="60" Header="{DynamicResource Shared-Valid}">
                                                <GridViewColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Horizontal">
                                                            <CheckBox IsChecked="{Binding (models:Tbl69FiSpecies.Valid)}" />
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </GridViewColumn.CellTemplate>
                                            </GridViewColumn>
                                            <GridViewColumn
                                            Width="200"
                                            DisplayMemberBinding="{Binding (models:Tbl69FiSpecies.Tbl66Genusses).GenusName}"
                                            Header="{DynamicResource Tbl66Genus-GenusName}" />
                                            <GridViewColumn
                                            Width="200"
                                            DisplayMemberBinding="{Binding (models:Tbl69FiSpecies.FiSpeciesName)}"
                                            Header="{DynamicResource Tbl69FiSpecies-FiSpeciesName}" />
                                            <GridViewColumn
                                            Width="200"
                                            DisplayMemberBinding="{Binding (models:Tbl69FiSpecies.Subspecies)}"
                                            Header="{DynamicResource Tbl69FiSpecies-Subspecies}" />
                                            <GridViewColumn
                                            Width="200"
                                            DisplayMemberBinding="{Binding (models:Tbl69FiSpecies.Divers)}"
                                            Header="{DynamicResource Tbl69FiSpecies-Divers}" />
                                            <GridViewColumn
                                            Width="200"
                                            DisplayMemberBinding="{Binding (models:Tbl69FiSpecies.Author)}"
                                            Header="{DynamicResource Shared-Author}" />
                                            <GridViewColumn
                                            Width="80"
                                            DisplayMemberBinding="{Binding (models:Tbl69FiSpecies.AuthorYear)}"
                                            Header="{DynamicResource Shared-AuthorYear}" />
            -->
            <!--  hier muss tblNames rein nach FiSpeciesID  -->
            <!--

                                        </GridView>
                                    </ListView.View>
                                </ListView>
                            </GroupBox>
                        </TabControl>
                    </TabItem>
                    <TabItem Header="{DynamicResource Database-MenuPlSpecies}">
                        <TabControl Margin="1">
            -->
            <!--  Bind the SelectionChanged event of the tab  -->
            <!--
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="SelectionChanged">
                                    <i:InvokeCommandAction Command="{Binding TabSelectionChangedPlSpeciesCommand}" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                            <GroupBox>
                                <ListView
                                x:Name="Tbl72PlSpeciessesList"
                                IsSynchronizedWithCurrentItem="True"
                                ItemsSource="{Binding PlSpeciessesCollection}"
                                ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                ScrollViewer.IsDeferredScrollingEnabled="True"
                                ScrollViewer.VerticalScrollBarVisibility="Auto"
                                VirtualizingStackPanel.IsVirtualizing="True"
                                VirtualizingStackPanel.VirtualizationMode="Recycling">

                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem">
                                            <Setter Property="Tag" Value="{Binding (models:Tbl72PlSpecies.PlSpeciesId)}" />
                                            <EventSetter Event="MouseDoubleClick" Handler="Tbl72PlSpeciessesList_DoubleClick" />
                                        </Style>
                                    </ListView.ItemContainerStyle>

                                    <ListView.View>
                                        <GridView>
                                            <GridViewColumn Width="Auto" Header="{DynamicResource Shared-CountId}">
                                                <GridViewColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <TextBlock
                                                        Width="100"
                                                        Text="{Binding (models:Tbl72PlSpecies.CountId)}"
                                                        TextAlignment="Left" />
                                                    </DataTemplate>
                                                </GridViewColumn.CellTemplate>
                                            </GridViewColumn>
                                            <GridViewColumn Width="Auto" Header="{DynamicResource Tbl72PlSpecies-PlSpeciesId}">
                                                <GridViewColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <TextBlock
                                                        Width="100"
                                                        Text="{Binding (models:Tbl72PlSpecies.PlSpeciesId)}"
                                                        TextAlignment="Left" />
                                                    </DataTemplate>
                                                </GridViewColumn.CellTemplate>
                                            </GridViewColumn>
                                            <GridViewColumn Width="60" Header="{DynamicResource Shared-Valid}">
                                                <GridViewColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Horizontal">
                                                            <CheckBox IsChecked="{Binding (models:Tbl72PlSpecies.Valid)}" />
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </GridViewColumn.CellTemplate>
                                            </GridViewColumn>
                                            <GridViewColumn
                                            Width="200"
                                            DisplayMemberBinding="{Binding (models:Tbl72PlSpecies.Tbl66Genusses).GenusName}"
                                            Header="{DynamicResource Tbl66Genus-GenusName}" />
                                            <GridViewColumn
                                            Width="200"
                                            DisplayMemberBinding="{Binding (models:Tbl72PlSpecies.PlSpeciesName)}"
                                            Header="{DynamicResource Tbl72PlSpecies-PlSpeciesName}" />
                                            <GridViewColumn
                                            Width="200"
                                            DisplayMemberBinding="{Binding (models:Tbl72PlSpecies.Subspecies)}"
                                            Header="{DynamicResource Tbl72PlSpecies-Subspecies}" />
                                            <GridViewColumn
                                            Width="200"
                                            DisplayMemberBinding="{Binding (models:Tbl72PlSpecies.Divers)}"
                                            Header="{DynamicResource Tbl72PlSpecies-Divers}" />
                                            <GridViewColumn
                                            Width="200"
                                            DisplayMemberBinding="{Binding (models:Tbl72PlSpecies.Author)}"
                                            Header="{DynamicResource Shared-Author}" />
                                            <GridViewColumn
                                            Width="80"
                                            DisplayMemberBinding="{Binding (models:Tbl72PlSpecies.AuthorYear)}"
                                            Header="{DynamicResource Shared-AuthorYear}" />
            -->
            <!--  hier muss tblNames rein nach PlSpeciesID  -->
            <!--

                                        </GridView>
                                    </ListView.View>
                                </ListView>
                            </GroupBox>
                        </TabControl>
                    </TabItem>
                    <TabItem Header="{DynamicResource Database-MenuTribus}">
                        <TabControl Margin="1" SelectedIndex="{Binding TabPageTribus}">
            -->
            <!--  Bind the SelectionChanged event of the tab  -->
            <!--
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="SelectionChanged">
                                    <i:InvokeCommandAction Command="{Binding TabSelectionChangedTribusCommand}" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                            <TabItem Header="{DynamicResource Database-MenuInfratribus}">
                                <GroupBox>
                                    <ListView
                                    x:Name="Tbl63InfratribussesList"
                                    IsSynchronizedWithCurrentItem="True"
                                    ItemsSource="{Binding InfratribussesCollection}"
                                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                    ScrollViewer.IsDeferredScrollingEnabled="True"
                                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                                    VirtualizingStackPanel.IsVirtualizing="True"
                                    VirtualizingStackPanel.VirtualizationMode="Recycling">

                                        <ListView.ItemContainerStyle>
                                            <Style TargetType="ListViewItem">
                                                <Setter Property="Tag" Value="{Binding (models:Tbl63Infratribus.SubtribusId)}" />
                                                <EventSetter Event="MouseDoubleClick" Handler="Tbl63InfratribussesList_DoubleClick" />
                                            </Style>
                                        </ListView.ItemContainerStyle>

                                        <ListView.View>
                                            <GridView>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Shared-CountId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl63Infratribus.CountId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Tbl63Infratribus-InfratribusId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl63Infratribus.InfratribusId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="60" Header="{DynamicResource Shared-Valid}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal">
                                                                <CheckBox IsChecked="{Binding (models:Tbl63Infratribus.Valid)}" />
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl63Infratribus.InfratribusName)}"
                                                Header="{DynamicResource Tbl63Infratribus-InfratribusName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl63Infratribus.Author)}"
                                                Header="{DynamicResource Shared-Author}" />
                                                <GridViewColumn
                                                Width="80"
                                                DisplayMemberBinding="{Binding (models:Tbl63Infratribus.AuthorYear)}"
                                                Header="{DynamicResource Shared-AuthorYear}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl63Infratribus.EngName)}"
                                                Header="{DynamicResource Shared-EngName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl63Infratribus.FraName)}"
                                                Header="{DynamicResource Shared-FraName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl63Infratribus.PorName)}"
                                                Header="{DynamicResource Shared-SpaName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl63Infratribus.GerName)}"
                                                Header="{DynamicResource Shared-GerName}" />
                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                </GroupBox>
                            </TabItem>
                            <TabItem Header="{DynamicResource Database-MenuSubtribus}">
                                <GroupBox>
                                    <ListView
                                    x:Name="Tbl60SubtribussesList"
                                    IsSynchronizedWithCurrentItem="True"
                                    ItemsSource="{Binding SubtribussesCollection}"
                                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                    ScrollViewer.IsDeferredScrollingEnabled="True"
                                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                                    VirtualizingStackPanel.IsVirtualizing="True"
                                    VirtualizingStackPanel.VirtualizationMode="Recycling">

                                        <ListView.ItemContainerStyle>
                                            <Style TargetType="ListViewItem">
                                                <Setter Property="Tag" Value="{Binding (models:Tbl60Subtribus.SubtribusId)}" />
                                                <EventSetter Event="MouseDoubleClick" Handler="Tbl60SubtribussesList_DoubleClick" />
                                            </Style>
                                        </ListView.ItemContainerStyle>

                                        <ListView.View>
                                            <GridView>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Shared-CountId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl60Subtribus.CountId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Tbl60Subtribus-SubtribusId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl60Subtribus.SubtribusId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="60" Header="{DynamicResource Shared-Valid}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal">
                                                                <CheckBox IsChecked="{Binding (models:Tbl60Subtribus.Valid)}" />
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl60Subtribus.SubtribusName)}"
                                                Header="{DynamicResource Tbl60Subtribus-SubtribusName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl60Subtribus.Author)}"
                                                Header="{DynamicResource Shared-Author}" />
                                                <GridViewColumn
                                                Width="80"
                                                DisplayMemberBinding="{Binding (models:Tbl60Subtribus.AuthorYear)}"
                                                Header="{DynamicResource Shared-AuthorYear}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl60Subtribus.EngName)}"
                                                Header="{DynamicResource Shared-EngName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl60Subtribus.FraName)}"
                                                Header="{DynamicResource Shared-FraName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl60Subtribus.PorName)}"
                                                Header="{DynamicResource Shared-SpaName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl60Subtribus.GerName)}"
                                                Header="{DynamicResource Shared-GerName}" />
                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                </GroupBox>
                            </TabItem>
                            <TabItem Header="{DynamicResource Database-MenuTribus}">
                                <GroupBox>
                                    <ListView
                                    x:Name="Tbl57TribussesList"
                                    IsSynchronizedWithCurrentItem="True"
                                    ItemsSource="{Binding TribussesCollection}"
                                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                    ScrollViewer.IsDeferredScrollingEnabled="True"
                                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                                    VirtualizingStackPanel.IsVirtualizing="True"
                                    VirtualizingStackPanel.VirtualizationMode="Recycling">

                                        <ListView.ItemContainerStyle>
                                            <Style TargetType="ListViewItem">
                                                <Setter Property="Tag" Value="{Binding (models:Tbl57Tribus.TribusId)}" />
                                                <EventSetter Event="MouseDoubleClick" Handler="Tbl57TribussesList_DoubleClick" />
                                            </Style>
                                        </ListView.ItemContainerStyle>

                                        <ListView.View>
                                            <GridView>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Shared-CountId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl57Tribus.CountId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Tbl57Tribus-TribusId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl57Tribus.TribusId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="60" Header="{DynamicResource Shared-Valid}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal">
                                                                <CheckBox IsChecked="{Binding (models:Tbl57Tribus.Valid)}" />
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl57Tribus.TribusName)}"
                                                Header="{DynamicResource Tbl57Tribus-TribusName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl57Tribus.Author)}"
                                                Header="{DynamicResource Shared-Author}" />
                                                <GridViewColumn
                                                Width="80"
                                                DisplayMemberBinding="{Binding (models:Tbl57Tribus.AuthorYear)}"
                                                Header="{DynamicResource Shared-AuthorYear}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl57Tribus.EngName)}"
                                                Header="{DynamicResource Shared-EngName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl57Tribus.FraName)}"
                                                Header="{DynamicResource Shared-FraName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl57Tribus.PorName)}"
                                                Header="{DynamicResource Shared-SpaName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl57Tribus.GerName)}"
                                                Header="{DynamicResource Shared-GerName}" />
                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                </GroupBox>
                            </TabItem>
                            <TabItem Header="{DynamicResource Database-MenuSupertribus}">
                                <GroupBox>
                                    <ListView
                                    x:Name="Tbl54SupertribussesList"
                                    IsSynchronizedWithCurrentItem="True"
                                    ItemsSource="{Binding SupertribussesCollection}"
                                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                    ScrollViewer.IsDeferredScrollingEnabled="True"
                                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                                    VirtualizingStackPanel.IsVirtualizing="True"
                                    VirtualizingStackPanel.VirtualizationMode="Recycling">

                                        <ListView.ItemContainerStyle>
                                            <Style TargetType="ListViewItem">
                                                <Setter Property="Tag" Value="{Binding (models:Tbl54Supertribus.SupertribusId)}" />
                                                <EventSetter Event="MouseDoubleClick" Handler="Tbl54SupertribussesList_DoubleClick" />
                                            </Style>
                                        </ListView.ItemContainerStyle>

                                        <ListView.View>
                                            <GridView>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Shared-CountId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl54Supertribus.CountId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Tbl54Supertribus-SupertribusId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl54Supertribus.SupertribusId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="60" Header="{DynamicResource Shared-Valid}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal">
                                                                <CheckBox IsChecked="{Binding (models:Tbl54Supertribus.Valid)}" />
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl54Supertribus.SupertribusName)}"
                                                Header="{DynamicResource Tbl54Supertribus-SupertribusName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl54Supertribus.Author)}"
                                                Header="{DynamicResource Shared-Author}" />
                                                <GridViewColumn
                                                Width="80"
                                                DisplayMemberBinding="{Binding (models:Tbl54Supertribus.AuthorYear)}"
                                                Header="{DynamicResource Shared-AuthorYear}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl54Supertribus.EngName)}"
                                                Header="{DynamicResource Shared-EngName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl54Supertribus.FraName)}"
                                                Header="{DynamicResource Shared-FraName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl54Supertribus.PorName)}"
                                                Header="{DynamicResource Shared-SpaName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl54Supertribus.GerName)}"
                                                Header="{DynamicResource Shared-GerName}" />
                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                </GroupBox>
                            </TabItem>
                        </TabControl>
                    </TabItem>
                    <TabItem Header="{DynamicResource Database-MenuFamily}">
                        <TabControl Margin="1" SelectedIndex="{Binding TabPageFamily}">
            -->
            <!--  Bind the SelectionChanged event of the tab  -->
            <!--
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="SelectionChanged">
                                    <i:InvokeCommandAction Command="{Binding TabSelectionChangedFamilyCommand}" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                            <TabItem Header="{DynamicResource Database-MenuInfrafamily}">
                                <GroupBox>
                                    <ListView
                                    x:Name="Tbl51InfrafamiliesList"
                                    IsSynchronizedWithCurrentItem="True"
                                    ItemsSource="{Binding InfrafamiliesCollection}"
                                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                    ScrollViewer.IsDeferredScrollingEnabled="True"
                                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                                    VirtualizingStackPanel.IsVirtualizing="True"
                                    VirtualizingStackPanel.VirtualizationMode="Recycling">

                                        <ListView.ItemContainerStyle>
                                            <Style TargetType="ListViewItem">
                                                <Setter Property="Tag" Value="{Binding (models:Tbl51Infrafamily.InfrafamilyId)}" />
                                                <EventSetter Event="MouseDoubleClick" Handler="Tbl51InfrafamiliesList_DoubleClick" />
                                            </Style>
                                        </ListView.ItemContainerStyle>

                                        <ListView.View>
                                            <GridView>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Shared-CountId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl51Infrafamily.CountId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Tbl51Infrafamily-InfrafamilyId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl51Infrafamily.InfrafamilyId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="60" Header="{DynamicResource Shared-Valid}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal">
                                                                <CheckBox IsChecked="{Binding (models:Tbl51Infrafamily.Valid)}" />
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl51Infrafamily.InfrafamilyName)}"
                                                Header="{DynamicResource Tbl51Infrafamily-InfrafamilyName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl51Infrafamily.Author)}"
                                                Header="{DynamicResource Shared-Author}" />
                                                <GridViewColumn
                                                Width="80"
                                                DisplayMemberBinding="{Binding (models:Tbl51Infrafamily.AuthorYear)}"
                                                Header="{DynamicResource Shared-AuthorYear}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl51Infrafamily.EngName)}"
                                                Header="{DynamicResource Shared-EngName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl51Infrafamily.FraName)}"
                                                Header="{DynamicResource Shared-FraName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl51Infrafamily.PorName)}"
                                                Header="{DynamicResource Shared-SpaName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl51Infrafamily.GerName)}"
                                                Header="{DynamicResource Shared-GerName}" />
                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                </GroupBox>
                            </TabItem>
                            <TabItem Header="{DynamicResource Database-MenuSubfamily}">
                                <GroupBox>
                                    <ListView
                                    x:Name="Tbl48SubfamiliesList"
                                    IsSynchronizedWithCurrentItem="True"
                                    ItemsSource="{Binding SubfamiliesCollection}"
                                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                    ScrollViewer.IsDeferredScrollingEnabled="True"
                                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                                    VirtualizingStackPanel.IsVirtualizing="True"
                                    VirtualizingStackPanel.VirtualizationMode="Recycling">

                                        <ListView.ItemContainerStyle>
                                            <Style TargetType="ListViewItem">
                                                <Setter Property="Tag" Value="{Binding (models:Tbl48Subfamily.SubfamilyId)}" />
                                                <EventSetter Event="MouseDoubleClick" Handler="Tbl48SubfamiliesList_DoubleClick" />
                                            </Style>
                                        </ListView.ItemContainerStyle>

                                        <ListView.View>
                                            <GridView>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Shared-CountId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl48Subfamily.CountId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Tbl48Subfamily-SubfamilyId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl48Subfamily.SubfamilyId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="60" Header="{DynamicResource Shared-Valid}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal">
                                                                <CheckBox IsChecked="{Binding (models:Tbl48Subfamily.Valid)}" />
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl48Subfamily.SubfamilyName)}"
                                                Header="{DynamicResource Tbl48Subfamily-SubfamilyName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl48Subfamily.Author)}"
                                                Header="{DynamicResource Shared-Author}" />
                                                <GridViewColumn
                                                Width="80"
                                                DisplayMemberBinding="{Binding (models:Tbl48Subfamily.AuthorYear)}"
                                                Header="{DynamicResource Shared-AuthorYear}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl48Subfamily.EngName)}"
                                                Header="{DynamicResource Shared-EngName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl48Subfamily.FraName)}"
                                                Header="{DynamicResource Shared-FraName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl48Subfamily.PorName)}"
                                                Header="{DynamicResource Shared-SpaName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl48Subfamily.GerName)}"
                                                Header="{DynamicResource Shared-GerName}" />
                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                </GroupBox>
                            </TabItem>
                            <TabItem Header="{DynamicResource Database-MenuFamily}">
                                <GroupBox>
                                    <ListView
                                    x:Name="Tbl45FamiliesList"
                                    IsSynchronizedWithCurrentItem="True"
                                    ItemsSource="{Binding FamiliesCollection}"
                                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                    ScrollViewer.IsDeferredScrollingEnabled="True"
                                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                                    VirtualizingStackPanel.IsVirtualizing="True"
                                    VirtualizingStackPanel.VirtualizationMode="Recycling">

                                        <ListView.ItemContainerStyle>
                                            <Style TargetType="ListViewItem">
                                                <Setter Property="Tag" Value="{Binding (models:Tbl45Family.FamilyId)}" />
                                                <EventSetter Event="MouseDoubleClick" Handler="Tbl45FamiliesList_DoubleClick" />
                                            </Style>
                                        </ListView.ItemContainerStyle>

                                        <ListView.View>
                                            <GridView>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Shared-CountId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl45Family.CountId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Tbl45Family-FamilyId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl45Family.FamilyId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="60" Header="{DynamicResource Shared-Valid}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal">
                                                                <CheckBox IsChecked="{Binding (models:Tbl45Family.Valid)}" />
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl45Family.FamilyName)}"
                                                Header="{DynamicResource Tbl45Family-FamilyName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl45Family.Author)}"
                                                Header="{DynamicResource Shared-Author}" />
                                                <GridViewColumn
                                                Width="80"
                                                DisplayMemberBinding="{Binding (models:Tbl45Family.AuthorYear)}"
                                                Header="{DynamicResource Shared-AuthorYear}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl45Family.EngName)}"
                                                Header="{DynamicResource Shared-EngName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl45Family.FraName)}"
                                                Header="{DynamicResource Shared-FraName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl45Family.PorName)}"
                                                Header="{DynamicResource Shared-SpaName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl45Family.GerName)}"
                                                Header="{DynamicResource Shared-GerName}" />
                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                </GroupBox>
                            </TabItem>
                            <TabItem Header="{DynamicResource Database-MenuSuperfamily}">
                                <GroupBox>
                                    <ListView
                                    x:Name="Tbl42SuperfamiliesList"
                                    IsSynchronizedWithCurrentItem="True"
                                    ItemsSource="{Binding SuperfamiliesCollection}"
                                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                    ScrollViewer.IsDeferredScrollingEnabled="True"
                                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                                    VirtualizingStackPanel.IsVirtualizing="True"
                                    VirtualizingStackPanel.VirtualizationMode="Recycling">

                                        <ListView.ItemContainerStyle>
                                            <Style TargetType="ListViewItem">
                                                <Setter Property="Tag" Value="{Binding (models:Tbl42Superfamily.SuperfamilyId)}" />
                                                <EventSetter Event="MouseDoubleClick" Handler="Tbl42SuperfamiliesList_DoubleClick" />
                                            </Style>
                                        </ListView.ItemContainerStyle>

                                        <ListView.View>
                                            <GridView>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Shared-CountId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl42Superfamily.CountId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Tbl42Superfamily-SuperfamilyId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl42Superfamily.SuperfamilyId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="60" Header="{DynamicResource Shared-Valid}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal">
                                                                <CheckBox IsChecked="{Binding (models:Tbl42Superfamily.Valid)}" />
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl42Superfamily.SuperfamilyName)}"
                                                Header="{DynamicResource Tbl42Superfamily-SuperfamilyName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl42Superfamily.Author)}"
                                                Header="{DynamicResource Shared-Author}" />
                                                <GridViewColumn
                                                Width="80"
                                                DisplayMemberBinding="{Binding (models:Tbl42Superfamily.AuthorYear)}"
                                                Header="{DynamicResource Shared-AuthorYear}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl42Superfamily.EngName)}"
                                                Header="{DynamicResource Shared-EngName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl42Superfamily.FraName)}"
                                                Header="{DynamicResource Shared-FraName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl42Superfamily.PorName)}"
                                                Header="{DynamicResource Shared-SpaName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl42Superfamily.GerName)}"
                                                Header="{DynamicResource Shared-GerName}" />
                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                </GroupBox>
                            </TabItem>
                        </TabControl>
                    </TabItem>
                    <TabItem Header="{DynamicResource Database-MenuOrdo}">
                        <TabControl Margin="1" SelectedIndex="{Binding TabPageOrdo}">
            -->
            <!--  Bind the SelectionChanged event of the tab  -->
            <!--
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="SelectionChanged">
                                    <i:InvokeCommandAction Command="{Binding TabSelectionChangedOrdoCommand}" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                            <TabItem Header="{DynamicResource Database-MenuInfraordo}">
                                <GroupBox>
                                    <ListView
                                    x:Name="Tbl39InfraordosList"
                                    IsSynchronizedWithCurrentItem="True"
                                    ItemsSource="{Binding InfraordosCollection}"
                                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                    ScrollViewer.IsDeferredScrollingEnabled="True"
                                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                                    VirtualizingStackPanel.IsVirtualizing="True"
                                    VirtualizingStackPanel.VirtualizationMode="Recycling">

                                        <ListView.ItemContainerStyle>
                                            <Style TargetType="ListViewItem">
                                                <Setter Property="Tag" Value="{Binding (models:Tbl39Infraordo.InfraordoId)}" />
                                                <EventSetter Event="MouseDoubleClick" Handler="Tbl39InfraordosList_DoubleClick" />
                                            </Style>
                                        </ListView.ItemContainerStyle>

                                        <ListView.View>
                                            <GridView>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Shared-CountId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl39Infraordo.CountId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Tbl39Infraordo-InfraordoId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl39Infraordo.InfraordoId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="60" Header="{DynamicResource Shared-Valid}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal">
                                                                <CheckBox IsChecked="{Binding (models:Tbl39Infraordo.Valid)}" />
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl39Infraordo.InfraordoName)}"
                                                Header="{DynamicResource Tbl39Infraordo-InfraordoName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl39Infraordo.Author)}"
                                                Header="{DynamicResource Shared-Author}" />
                                                <GridViewColumn
                                                Width="80"
                                                DisplayMemberBinding="{Binding (models:Tbl39Infraordo.AuthorYear)}"
                                                Header="{DynamicResource Shared-AuthorYear}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl39Infraordo.EngName)}"
                                                Header="{DynamicResource Shared-EngName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl39Infraordo.FraName)}"
                                                Header="{DynamicResource Shared-FraName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl39Infraordo.PorName)}"
                                                Header="{DynamicResource Shared-SpaName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl39Infraordo.GerName)}"
                                                Header="{DynamicResource Shared-GerName}" />
                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                </GroupBox>
                            </TabItem>
                            <TabItem Header="{DynamicResource Database-MenuSubordo}">
                                <GroupBox>
                                    <ListView
                                    x:Name="Tbl36SubordosList"
                                    IsSynchronizedWithCurrentItem="True"
                                    ItemsSource="{Binding SubordosCollection}"
                                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                    ScrollViewer.IsDeferredScrollingEnabled="True"
                                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                                    VirtualizingStackPanel.IsVirtualizing="True"
                                    VirtualizingStackPanel.VirtualizationMode="Recycling">

                                        <ListView.ItemContainerStyle>
                                            <Style TargetType="ListViewItem">
                                                <Setter Property="Tag" Value="{Binding (models:Tbl36Subordo.SubordoId)}" />
                                                <EventSetter Event="MouseDoubleClick" Handler="Tbl36SubordosList_DoubleClick" />
                                            </Style>
                                        </ListView.ItemContainerStyle>

                                        <ListView.View>
                                            <GridView>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Shared-CountId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl36Subordo.CountId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Tbl36Subordo-SubordoId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl36Subordo.SubordoId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="60" Header="{DynamicResource Shared-Valid}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal">
                                                                <CheckBox IsChecked="{Binding (models:Tbl36Subordo.Valid)}" />
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl36Subordo.SubordoName)}"
                                                Header="{DynamicResource Tbl36Subordo-SubordoName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl36Subordo.Author)}"
                                                Header="{DynamicResource Shared-Author}" />
                                                <GridViewColumn
                                                Width="80"
                                                DisplayMemberBinding="{Binding (models:Tbl36Subordo.AuthorYear)}"
                                                Header="{DynamicResource Shared-AuthorYear}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl36Subordo.EngName)}"
                                                Header="{DynamicResource Shared-EngName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl36Subordo.FraName)}"
                                                Header="{DynamicResource Shared-FraName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl36Subordo.PorName)}"
                                                Header="{DynamicResource Shared-SpaName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl36Subordo.GerName)}"
                                                Header="{DynamicResource Shared-GerName}" />
                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                </GroupBox>
                            </TabItem>
                            <TabItem Header="{DynamicResource Database-MenuOrdo}">
                                <GroupBox>
                                    <ListView
                                    x:Name="Tbl33OrdosList"
                                    IsSynchronizedWithCurrentItem="True"
                                    ItemsSource="{Binding OrdosCollection}"
                                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                    ScrollViewer.IsDeferredScrollingEnabled="True"
                                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                                    VirtualizingStackPanel.IsVirtualizing="True"
                                    VirtualizingStackPanel.VirtualizationMode="Recycling">

                                        <ListView.ItemContainerStyle>
                                            <Style TargetType="ListViewItem">
                                                <Setter Property="Tag" Value="{Binding (models:Tbl33Ordo.OrdoId)}" />
                                                <EventSetter Event="MouseDoubleClick" Handler="Tbl33OrdosList_DoubleClick" />
                                            </Style>
                                        </ListView.ItemContainerStyle>

                                        <ListView.View>
                                            <GridView>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Shared-CountId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl33Ordo.CountId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Tbl33Ordo-OrdoId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl33Ordo.OrdoId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="60" Header="{DynamicResource Shared-Valid}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal">
                                                                <CheckBox IsChecked="{Binding (models:Tbl33Ordo.Valid)}" />
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl33Ordo.OrdoName)}"
                                                Header="{DynamicResource Tbl33Ordo-OrdoName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl33Ordo.Author)}"
                                                Header="{DynamicResource Shared-Author}" />
                                                <GridViewColumn
                                                Width="80"
                                                DisplayMemberBinding="{Binding (models:Tbl33Ordo.AuthorYear)}"
                                                Header="{DynamicResource Shared-AuthorYear}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl33Ordo.EngName)}"
                                                Header="{DynamicResource Shared-EngName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl33Ordo.FraName)}"
                                                Header="{DynamicResource Shared-FraName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl33Ordo.PorName)}"
                                                Header="{DynamicResource Shared-SpaName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl33Ordo.GerName)}"
                                                Header="{DynamicResource Shared-GerName}" />
                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                </GroupBox>
                            </TabItem>
                            <TabItem Header="{DynamicResource Database-MenuLegio}">
                                <GroupBox>
                                    <ListView
                                    x:Name="Tbl30LegiosList"
                                    IsSynchronizedWithCurrentItem="True"
                                    ItemsSource="{Binding LegiosCollection}"
                                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                    ScrollViewer.IsDeferredScrollingEnabled="True"
                                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                                    VirtualizingStackPanel.IsVirtualizing="True"
                                    VirtualizingStackPanel.VirtualizationMode="Recycling">

                                        <ListView.ItemContainerStyle>
                                            <Style TargetType="ListViewItem">
                                                <Setter Property="Tag" Value="{Binding (models:Tbl30Legio.LegioId)}" />
                                                <EventSetter Event="MouseDoubleClick" Handler="Tbl30LegiosList_DoubleClick" />
                                            </Style>
                                        </ListView.ItemContainerStyle>

                                        <ListView.View>
                                            <GridView>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Shared-CountId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl30Legio.CountId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Tbl30Legio-LegioId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl30Legio.LegioId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="60" Header="{DynamicResource Shared-Valid}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal">
                                                                <CheckBox IsChecked="{Binding (models:Tbl30Legio.Valid)}" />
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl30Legio.LegioName)}"
                                                Header="{DynamicResource Tbl30Legio-LegioName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl30Legio.Author)}"
                                                Header="{DynamicResource Shared-Author}" />
                                                <GridViewColumn
                                                Width="80"
                                                DisplayMemberBinding="{Binding (models:Tbl30Legio.AuthorYear)}"
                                                Header="{DynamicResource Shared-AuthorYear}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl30Legio.EngName)}"
                                                Header="{DynamicResource Shared-EngName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl30Legio.FraName)}"
                                                Header="{DynamicResource Shared-FraName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl30Legio.PorName)}"
                                                Header="{DynamicResource Shared-SpaName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl30Legio.GerName)}"
                                                Header="{DynamicResource Shared-GerName}" />
                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                </GroupBox>
                            </TabItem>
                        </TabControl>
                    </TabItem>
                    <TabItem Header="{DynamicResource Database-MenuClass}">
                        <TabControl Margin="1" SelectedIndex="{Binding TabPageClass}">
            -->
            <!--  Bind the SelectionChanged event of the tab  -->
            <!--
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="SelectionChanged">
                                    <i:InvokeCommandAction Command="{Binding TabSelectionChangedClassCommand}" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                            <TabItem Header="{DynamicResource Database-MenuInfraclass}">
                                <GroupBox>
                                    <ListView
                                    x:Name="Tbl27InfraclassesList"
                                    IsSynchronizedWithCurrentItem="True"
                                    ItemsSource="{Binding InfraclassesCollection}"
                                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                    ScrollViewer.IsDeferredScrollingEnabled="True"
                                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                                    VirtualizingStackPanel.IsVirtualizing="True"
                                    VirtualizingStackPanel.VirtualizationMode="Recycling">

                                        <ListView.ItemContainerStyle>
                                            <Style TargetType="ListViewItem">
                                                <Setter Property="Tag" Value="{Binding (models:Tbl27Infraclass.InfraclassId)}" />
                                                <EventSetter Event="MouseDoubleClick" Handler="Tbl27InfraclassesList_DoubleClick" />
                                            </Style>
                                        </ListView.ItemContainerStyle>

                                        <ListView.View>
                                            <GridView>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Shared-CountId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl27Infraclass.CountId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Tbl27Infraclass-InfraclassId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl27Infraclass.InfraclassId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="60" Header="{DynamicResource Shared-Valid}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal">
                                                                <CheckBox IsChecked="{Binding (models:Tbl27Infraclass.Valid)}" />
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl27Infraclass.InfraclassName)}"
                                                Header="{DynamicResource Tbl27Infraclass-InfraclassName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl27Infraclass.Author)}"
                                                Header="{DynamicResource Shared-Author}" />
                                                <GridViewColumn
                                                Width="80"
                                                DisplayMemberBinding="{Binding (models:Tbl27Infraclass.AuthorYear)}"
                                                Header="{DynamicResource Shared-AuthorYear}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl27Infraclass.EngName)}"
                                                Header="{DynamicResource Shared-EngName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl27Infraclass.FraName)}"
                                                Header="{DynamicResource Shared-FraName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl27Infraclass.PorName)}"
                                                Header="{DynamicResource Shared-SpaName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl27Infraclass.GerName)}"
                                                Header="{DynamicResource Shared-GerName}" />
                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                </GroupBox>
                            </TabItem>
                            <TabItem Header="{DynamicResource Database-MenuSubclass}">
                                <GroupBox>
                                    <ListView
                                    x:Name="Tbl24SubclassesList"
                                    IsSynchronizedWithCurrentItem="True"
                                    ItemsSource="{Binding SubclassesCollection}"
                                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                    ScrollViewer.IsDeferredScrollingEnabled="True"
                                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                                    VirtualizingStackPanel.IsVirtualizing="True"
                                    VirtualizingStackPanel.VirtualizationMode="Recycling">

                                        <ListView.ItemContainerStyle>
                                            <Style TargetType="ListViewItem">
                                                <Setter Property="Tag" Value="{Binding (models:Tbl24Subclass.SubclassId)}" />
                                                <EventSetter Event="MouseDoubleClick" Handler="Tbl24SubclassesList_DoubleClick" />
                                            </Style>
                                        </ListView.ItemContainerStyle>

                                        <ListView.View>
                                            <GridView>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Shared-CountId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl24Subclass.CountId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Tbl24Subclass-SubclassId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl24Subclass.SubclassId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="60" Header="{DynamicResource Shared-Valid}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal">
                                                                <CheckBox IsChecked="{Binding (models:Tbl24Subclass.Valid)}" />
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl24Subclass.SubclassName)}"
                                                Header="{DynamicResource Tbl24Subclass-SubclassName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl24Subclass.Author)}"
                                                Header="{DynamicResource Shared-Author}" />
                                                <GridViewColumn
                                                Width="80"
                                                DisplayMemberBinding="{Binding (models:Tbl24Subclass.AuthorYear)}"
                                                Header="{DynamicResource Shared-AuthorYear}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl24Subclass.EngName)}"
                                                Header="{DynamicResource Shared-EngName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl24Subclass.FraName)}"
                                                Header="{DynamicResource Shared-FraName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl24Subclass.PorName)}"
                                                Header="{DynamicResource Shared-SpaName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl24Subclass.GerName)}"
                                                Header="{DynamicResource Shared-GerName}" />
                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                </GroupBox>
                            </TabItem>
                            <TabItem Header="{DynamicResource Database-MenuClass}">
                                <GroupBox>
                                    <ListView
                                    x:Name="Tbl21ClassesList"
                                    IsSynchronizedWithCurrentItem="True"
                                    ItemsSource="{Binding ClassesCollection}"
                                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                    ScrollViewer.IsDeferredScrollingEnabled="True"
                                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                                    VirtualizingStackPanel.IsVirtualizing="True"
                                    VirtualizingStackPanel.VirtualizationMode="Recycling">

                                        <ListView.ItemContainerStyle>
                                            <Style TargetType="ListViewItem">
                                                <Setter Property="Tag" Value="{Binding (models:Tbl21Class.ClassId)}" />
                                                <EventSetter Event="MouseDoubleClick" Handler="Tbl21ClassesList_DoubleClick" />
                                            </Style>
                                        </ListView.ItemContainerStyle>

                                        <ListView.View>
                                            <GridView>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Shared-CountId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl21Class.CountId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Tbl21Class-ClassId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl21Class.ClassId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="60" Header="{DynamicResource Shared-Valid}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal">
                                                                <CheckBox IsChecked="{Binding (models:Tbl21Class.Valid)}" />
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl21Class.ClassName)}"
                                                Header="{DynamicResource Tbl21Class-ClassName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl21Class.Author)}"
                                                Header="{DynamicResource Shared-Author}" />
                                                <GridViewColumn
                                                Width="80"
                                                DisplayMemberBinding="{Binding (models:Tbl21Class.AuthorYear)}"
                                                Header="{DynamicResource Shared-AuthorYear}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl21Class.EngName)}"
                                                Header="{DynamicResource Shared-EngName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl21Class.FraName)}"
                                                Header="{DynamicResource Shared-FraName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl21Class.PorName)}"
                                                Header="{DynamicResource Shared-SpaName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl21Class.GerName)}"
                                                Header="{DynamicResource Shared-GerName}" />
                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                </GroupBox>
                            </TabItem>
                            <TabItem Header="{DynamicResource Database-MenuSuperclass}">
                                <GroupBox>
                                    <ListView
                                    x:Name="Tbl18SuperclassesList"
                                    IsSynchronizedWithCurrentItem="True"
                                    ItemsSource="{Binding SuperclassesCollection}"
                                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                    ScrollViewer.IsDeferredScrollingEnabled="True"
                                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                                    VirtualizingStackPanel.IsVirtualizing="True"
                                    VirtualizingStackPanel.VirtualizationMode="Recycling">

                                        <ListView.ItemContainerStyle>
                                            <Style TargetType="ListViewItem">
                                                <Setter Property="Tag" Value="{Binding (models:Tbl18Superclass.SuperclassId)}" />
                                                <EventSetter Event="MouseDoubleClick" Handler="Tbl18SuperclassesList_DoubleClick" />
                                            </Style>
                                        </ListView.ItemContainerStyle>

                                        <ListView.View>
                                            <GridView>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Shared-CountId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl18Superclass.CountId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Tbl18Superclass-SuperclassId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl18Superclass.SuperclassId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="60" Header="{DynamicResource Shared-Valid}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal">
                                                                <CheckBox IsChecked="{Binding (models:Tbl18Superclass.Valid)}" />
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl18Superclass.SuperclassName)}"
                                                Header="{DynamicResource Tbl18Superclass-SuperclassName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl18Superclass.Author)}"
                                                Header="{DynamicResource Shared-Author}" />
                                                <GridViewColumn
                                                Width="80"
                                                DisplayMemberBinding="{Binding (models:Tbl18Superclass.AuthorYear)}"
                                                Header="{DynamicResource Shared-AuthorYear}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl18Superclass.EngName)}"
                                                Header="{DynamicResource Shared-EngName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl18Superclass.FraName)}"
                                                Header="{DynamicResource Shared-FraName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl18Superclass.PorName)}"
                                                Header="{DynamicResource Shared-SpaName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl18Superclass.GerName)}"
                                                Header="{DynamicResource Shared-GerName}" />
                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                </GroupBox>
                            </TabItem>
                        </TabControl>
                    </TabItem>
                    <TabItem Header="{DynamicResource Database-MenuDivision}">
                        <TabControl Margin="1" SelectedIndex="{Binding TabPageDivision}">
            -->
            <!--  Bind the SelectionChanged event of the tab  -->
            <!--
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="SelectionChanged">
                                    <i:InvokeCommandAction Command="{Binding TabSelectionChangedDivisionCommand}" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                            <TabItem Header="{DynamicResource Database-MenuSubdivision}">
                                <GroupBox>
                                    <ListView
                                    x:Name="Tbl15SubdivisionsList"
                                    IsSynchronizedWithCurrentItem="True"
                                    ItemsSource="{Binding SubdivisionsCollection}"
                                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                    ScrollViewer.IsDeferredScrollingEnabled="True"
                                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                                    VirtualizingStackPanel.IsVirtualizing="True"
                                    VirtualizingStackPanel.VirtualizationMode="Recycling">

                                        <ListView.ItemContainerStyle>
                                            <Style TargetType="ListViewItem">
                                                <Setter Property="Tag" Value="{Binding (models:Tbl15Subdivision.SubdivisionId)}" />
                                                <EventSetter Event="MouseDoubleClick" Handler="Tbl15SubdivisionsList_DoubleClick" />
                                            </Style>
                                        </ListView.ItemContainerStyle>

                                        <ListView.View>
                                            <GridView>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Shared-CountId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl15Subdivision.CountId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Tbl15Subdivision-SubdivisionId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl15Subdivision.SubdivisionId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="60" Header="{DynamicResource Shared-Valid}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal">
                                                                <CheckBox IsChecked="{Binding (models:Tbl15Subdivision.Valid)}" />
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl15Subdivision.SubdivisionName)}"
                                                Header="{DynamicResource Tbl15Subdivision-SubdivisionName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl15Subdivision.Author)}"
                                                Header="{DynamicResource Shared-Author}" />
                                                <GridViewColumn
                                                Width="80"
                                                DisplayMemberBinding="{Binding (models:Tbl15Subdivision.AuthorYear)}"
                                                Header="{DynamicResource Shared-AuthorYear}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl12Subphylum.EngName)}"
                                                Header="{DynamicResource Shared-EngName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl15Subdivision.FraName)}"
                                                Header="{DynamicResource Shared-FraName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl15Subdivision.PorName)}"
                                                Header="{DynamicResource Shared-SpaName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl15Subdivision.GerName)}"
                                                Header="{DynamicResource Shared-GerName}" />
                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                </GroupBox>
                            </TabItem>
                            <TabItem Header="{DynamicResource Database-MenuDivision}">
                                <ScrollViewer VerticalScrollBarVisibility="Visible">
                                    <GroupBox>
                                        <ListView
                                        x:Name="Tbl09DivisionsList"
                                        IsSynchronizedWithCurrentItem="True"
                                        ItemsSource="{Binding DivisionsCollection}"
                                        ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                        ScrollViewer.IsDeferredScrollingEnabled="True"
                                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                                        VirtualizingStackPanel.IsVirtualizing="True"
                                        VirtualizingStackPanel.VirtualizationMode="Recycling">

                                            <ListView.ItemContainerStyle>
                                                <Style TargetType="ListViewItem">
                                                    <Setter Property="Tag" Value="{Binding (models:Tbl09Division.DivisionId)}" />
                                                    <EventSetter Event="MouseDoubleClick" Handler="Tbl09DivisionsList_DoubleClick" />
                                                </Style>
                                            </ListView.ItemContainerStyle>

                                            <ListView.View>
                                                <GridView>
                                                    <GridViewColumn Width="Auto" Header="{DynamicResource Shared-CountId}">
                                                        <GridViewColumn.CellTemplate>
                                                            <DataTemplate>
                                                                <TextBlock
                                                                Width="100"
                                                                Text="{Binding (models:Tbl09Division.CountId)}"
                                                                TextAlignment="Left" />
                                                            </DataTemplate>
                                                        </GridViewColumn.CellTemplate>
                                                    </GridViewColumn>
                                                    <GridViewColumn Width="Auto" Header="{DynamicResource Tbl09Division-DivisionId}">
                                                        <GridViewColumn.CellTemplate>
                                                            <DataTemplate>
                                                                <TextBlock
                                                                Width="100"
                                                                Text="{Binding (models:Tbl09Division.DivisionId)}"
                                                                TextAlignment="Left" />
                                                            </DataTemplate>
                                                        </GridViewColumn.CellTemplate>
                                                    </GridViewColumn>
                                                    <GridViewColumn Width="60" Header="{DynamicResource Shared-Valid}">
                                                        <GridViewColumn.CellTemplate>
                                                            <DataTemplate>
                                                                <StackPanel Orientation="Horizontal">
                                                                    <CheckBox IsChecked="{Binding (models:Tbl09Division.Valid)}" />
                                                                </StackPanel>
                                                            </DataTemplate>
                                                        </GridViewColumn.CellTemplate>
                                                    </GridViewColumn>
                                                    <GridViewColumn
                                                    Width="200"
                                                    DisplayMemberBinding="{Binding (models:Tbl09Division.DivisionName)}"
                                                    Header="{DynamicResource Tbl09Division-DivisionName}" />
                                                    <GridViewColumn
                                                    Width="200"
                                                    DisplayMemberBinding="{Binding (models:Tbl09Division.Author)}"
                                                    Header="{DynamicResource Shared-Author}" />
                                                    <GridViewColumn
                                                    Width="80"
                                                    DisplayMemberBinding="{Binding (models:Tbl09Division.AuthorYear)}"
                                                    Header="{DynamicResource Shared-AuthorYear}" />
                                                    <GridViewColumn
                                                    Width="200"
                                                    DisplayMemberBinding="{Binding (models:Tbl09Division.EngName)}"
                                                    Header="{DynamicResource Shared-EngName}" />
                                                    <GridViewColumn
                                                    Width="200"
                                                    DisplayMemberBinding="{Binding (models:Tbl09Division.FraName)}"
                                                    Header="{DynamicResource Shared-FraName}" />
                                                    <GridViewColumn
                                                    Width="200"
                                                    DisplayMemberBinding="{Binding (models:Tbl09Division.PorName)}"
                                                    Header="{DynamicResource Shared-SpaName}" />
                                                    <GridViewColumn
                                                    Width="200"
                                                    DisplayMemberBinding="{Binding (models:Tbl09Division.GerName)}"
                                                    Header="{DynamicResource Shared-GerName}" />
                                                </GridView>
                                            </ListView.View>
                                        </ListView>
                                    </GroupBox>
                                </ScrollViewer>
                            </TabItem>
                        </TabControl>
                    </TabItem>
                    <TabItem Header="{DynamicResource Database-MenuPhylum}">
                        <TabControl Margin="1" SelectedIndex="{Binding TabPagePhylum}">
            -->
            <!--  Bind the SelectionChanged event of the tab  -->
            <!--
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="SelectionChanged">
                                    <i:InvokeCommandAction Command="{Binding TabSelectionChangedPhylumCommand}" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                            <TabItem Header="{DynamicResource Database-MenuSubphylum}">
                                <GroupBox>
                                    <ListView
                                    x:Name="Tbl12SubphylumsList"
                                    IsSynchronizedWithCurrentItem="True"
                                    ItemsSource="{Binding SubphylumsCollection}"
                                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                    ScrollViewer.IsDeferredScrollingEnabled="True"
                                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                                    VirtualizingStackPanel.IsVirtualizing="True"
                                    VirtualizingStackPanel.VirtualizationMode="Recycling">

                                        <ListView.ItemContainerStyle>
                                            <Style TargetType="ListViewItem">
                                                <Setter Property="Tag" Value="{Binding (models:Tbl12Subphylum.SubphylumId)}" />
                                                <EventSetter Event="MouseDoubleClick" Handler="Tbl12SubphylumsList_DoubleClick" />
                                            </Style>
                                        </ListView.ItemContainerStyle>

                                        <ListView.View>
                                            <GridView>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Shared-CountId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl12Subphylum.CountId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Tbl12Subphylum-SubphylumId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl12Subphylum.SubphylumId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="60" Header="{DynamicResource Shared-Valid}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal">
                                                                <CheckBox IsChecked="{Binding (models:Tbl12Subphylum.Valid)}" />
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl12Subphylum.SubphylumName)}"
                                                Header="{DynamicResource Tbl12Subphylum-SubphylumName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl12Subphylum.Author)}"
                                                Header="{DynamicResource Shared-Author}" />
                                                <GridViewColumn
                                                Width="80"
                                                DisplayMemberBinding="{Binding (models:Tbl12Subphylum.AuthorYear)}"
                                                Header="{DynamicResource Shared-AuthorYear}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl12Subphylum.EngName)}"
                                                Header="{DynamicResource Shared-EngName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl12Subphylum.FraName)}"
                                                Header="{DynamicResource Shared-FraName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl12Subphylum.PorName)}"
                                                Header="{DynamicResource Shared-SpaName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl12Subphylum.GerName)}"
                                                Header="{DynamicResource Shared-GerName}" />
                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                </GroupBox>
                            </TabItem>
                            <TabItem Header="{DynamicResource Database-MenuPhylum}">
                                <GroupBox>
                                    <ListView
                                    x:Name="Tbl06PhylumsList"
                                    IsSynchronizedWithCurrentItem="True"
                                    ItemsSource="{Binding PhylumsCollection}"
                                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                    ScrollViewer.IsDeferredScrollingEnabled="True"
                                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                                    Tag="{Binding PhylumId}"
                                    VirtualizingStackPanel.IsVirtualizing="True"
                                    VirtualizingStackPanel.VirtualizationMode="Recycling">

                                        <ListView.ItemContainerStyle>
                                            <Style TargetType="ListViewItem">
                                                <Setter Property="Tag" Value="{Binding (models:Tbl06Phylum.PhylumId)}" />
                                                <EventSetter Event="MouseDoubleClick" Handler="Tbl06PhylumsList_DoubleClick" />
                                            </Style>
                                        </ListView.ItemContainerStyle>

                                        <ListView.View>
                                            <GridView>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Shared-CountId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl06Phylum.CountId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="Auto" Header="{DynamicResource Tbl06Phylum-PhylumId}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                            Width="100"
                                                            Text="{Binding (models:Tbl06Phylum.PhylumId)}"
                                                            TextAlignment="Left" />
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn Width="60" Header="{DynamicResource Shared-Valid}">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal">
                                                                <CheckBox IsChecked="{Binding (models:Tbl06Phylum.Valid)}" />
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl06Phylum.PhylumName)}"
                                                Header="{DynamicResource Tbl06Phylum-PhylumName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl06Phylum.Author)}"
                                                Header="{DynamicResource Shared-Author}" />
                                                <GridViewColumn
                                                Width="80"
                                                DisplayMemberBinding="{Binding (models:Tbl06Phylum.AuthorYear)}"
                                                Header="{DynamicResource Shared-AuthorYear}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl06Phylum.EngName)}"
                                                Header="{DynamicResource Shared-EngName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl06Phylum.FraName)}"
                                                Header="{DynamicResource Shared-FraName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl06Phylum.PorName)}"
                                                Header="{DynamicResource Shared-SpaName}" />
                                                <GridViewColumn
                                                Width="200"
                                                DisplayMemberBinding="{Binding (models:Tbl06Phylum.GerName)}"
                                                Header="{DynamicResource Shared-GerName}" />
                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                </GroupBox>
                            </TabItem>
                        </TabControl>
                    </TabItem>
                    <TabItem Header="{DynamicResource Database-MenuRegnum}">
                        <TabControl Margin="1">
            -->
            <!--  Bind the SelectionChanged event of the tab  -->
            <!--
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="SelectionChanged">
                                    <i:InvokeCommandAction Command="{Binding TabSelectionChangedRegnumCommand}" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                            <GroupBox>
                                <ListView
                                x:Name="Tbl03RegnumsList"
                                IsSynchronizedWithCurrentItem="True"
                                ItemsSource="{Binding RegnumsCollection}"
                                ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                ScrollViewer.IsDeferredScrollingEnabled="True"
                                ScrollViewer.VerticalScrollBarVisibility="Auto"
                                VirtualizingStackPanel.IsVirtualizing="True"
                                VirtualizingStackPanel.VirtualizationMode="Recycling">

                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem">
                                            <Setter Property="Tag" Value="{Binding (models:Tbl03Regnum.RegnumId)}" />
                                            <EventSetter Event="MouseDoubleClick" Handler="Tbl03RegnumsList_DoubleClick" />
                                        </Style>
                                    </ListView.ItemContainerStyle>

                                    <ListView.View>
                                        <GridView>
                                            <GridViewColumn Width="Auto" Header="{DynamicResource Shared-CountId}">
                                                <GridViewColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <TextBlock
                                                        Width="100"
                                                        Text="{Binding (models:Tbl03Regnum.CountId)}"
                                                        TextAlignment="Left" />
                                                    </DataTemplate>
                                                </GridViewColumn.CellTemplate>
                                            </GridViewColumn>
                                            <GridViewColumn Width="Auto" Header="{DynamicResource Tbl03Regnum-RegnumId}">
                                                <GridViewColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <TextBlock
                                                        Width="100"
                                                        Text="{Binding (models:Tbl03Regnum.RegnumId)}"
                                                        TextAlignment="Left" />
                                                    </DataTemplate>
                                                </GridViewColumn.CellTemplate>
                                            </GridViewColumn>
                                            <GridViewColumn Width="60" Header="{DynamicResource Shared-Valid}">
                                                <GridViewColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Horizontal">
                                                            <CheckBox IsChecked="{Binding (models:Tbl03Regnum.Valid)}" />
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </GridViewColumn.CellTemplate>
                                            </GridViewColumn>
                                            <GridViewColumn
                                            Width="200"
                                            DisplayMemberBinding="{Binding (models:Tbl03Regnum.RegnumName)}"
                                            Header="{DynamicResource Tbl03Regnum-RegnumName}" />
                                            <GridViewColumn
                                            Width="200"
                                            DisplayMemberBinding="{Binding (models:Tbl03Regnum.Subregnum)}"
                                            Header="{DynamicResource Tbl03Regnum-Subregnum}" />
                                            <GridViewColumn
                                            Width="200"
                                            DisplayMemberBinding="{Binding (models:Tbl03Regnum.Author)}"
                                            Header="{DynamicResource Shared-Author}" />
                                            <GridViewColumn
                                            Width="80"
                                            DisplayMemberBinding="{Binding (models:Tbl03Regnum.AuthorYear)}"
                                            Header="{DynamicResource Shared-AuthorYear}" />
                                            <GridViewColumn
                                            Width="200"
                                            DisplayMemberBinding="{Binding (models:Tbl03Regnum.EngName)}"
                                            Header="{DynamicResource Shared-EngName}" />
                                            <GridViewColumn
                                            Width="200"
                                            DisplayMemberBinding="{Binding (models:Tbl03Regnum.FraName)}"
                                            Header="{DynamicResource Shared-FraName}" />
                                            <GridViewColumn
                                            Width="200"
                                            DisplayMemberBinding="{Binding (models:Tbl03Regnum.PorName)}"
                                            Header="{DynamicResource Shared-SpaName}" />
                                            <GridViewColumn
                                            Width="200"
                                            DisplayMemberBinding="{Binding (models:Tbl03Regnum.GerName)}"
                                            Header="{DynamicResource Shared-GerName}" />
                                        </GridView>
                                    </ListView.View>
                                </ListView>
                            </GroupBox>
                        </TabControl>
                    </TabItem>
                </TabControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>-->
        </Grid>
    </Grid>
</UserControl>
